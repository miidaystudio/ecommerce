# Base URL of the NestJS API (admin fetches go through lib/api/*)
NEXT_PUBLIC_API_URL=http://localhost:4000/api

# Name of the refresh cookie the API sets; middleware reads it for the soft route gate.
# Must match the API's REFRESH_COOKIE_NAME.
REFRESH_COOKIE_NAME=refresh_token
